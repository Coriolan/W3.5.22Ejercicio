<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.w3522project">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.w3522project.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.w3522project.Favoritos"
            android:parentActivityName="com.example.w3522project.MainActivity">

            <!-- SOPORTE PARA VERSIONES 4.0 O MENORES -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.w3522project.MainActivity" />
        </activity>
        <activity android:name="com.example.w3522project.About"
            android:parentActivityName="com.example.w3522project.MainActivity">
            <!-- SOPORTE PARA VERSIONES 4.0 O MENORES -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.w3522project.MainActivity" />
        </activity>
        <activity android:name="com.example.w3522project.Contacto"
            android:parentActivityName="com.example.w3522project.MainActivity">
            <!-- SOPORTE PARA VERSIONES 4.0 O MENORES -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.w3522project.MainActivity" />
        </activity>
    </application>

</manifest>